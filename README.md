# GCU Solar Dashboard V2
Replace the existing `index.html` and `vercel.json` in your GitHub repository with these files and commit.
Vercel will automatically redeploy the existing URL.

Current data is based on the supplied Excel history. Connect inverter/BMS API or Modbus for true live readings and battery SOC.
